# 桨板直线划行 AI 陪练

## 项目定位

面向桨板课程学生的网页应用。学生可手持桨或木棍进行桨板直线划行的陆上模拟练习，由系统实时识别动作并提供反馈。

## 主要使用设备

当前阶段主要面向 **电脑端浏览器**（Windows / macOS），使用外接或内置摄像头。

手机和平板端暂不作为当前阶段验收要求，但保留未来移动端适配的可行性。

## 技术栈

- React 19 + TypeScript + Vite
- MediaPipe Tasks Vision (Pose Landmarker)
- HTML Canvas + 普通 CSS
- 纯前端，无后端服务器、无数据库、无登录系统

## V0 边界说明

- 不识别木棍或桨
- 不判断动作正确或错误
- 不计算关节角度
- 不计算双手距离
- 不进行动作计数或评分
- 不保存视频或上传用户数据
- 不添加登录注册

## 版本记录

### V0 — 人体姿态识别与稳定性优化（已通过）
- React + TypeScript + Vite 项目搭建
- 摄像头控制（前置优先）与 MediaPipe Pose Landmarker 集成
- Canvas 骨架绘制（33 关键点 + 连接线）
- 镜像方向一致（CSS scaleX(-1)）
- 同帧推理去重（video.currentTime 帧缓存）
- 上肢 EMA 轻量平滑（肩/肘/腕，原始数据与显示数据分离）
- 5 种状态显示、FPS、响应式布局、资源释放

### V1 — 基础人体指标识别（已基本通过）
- 人体完整入镜（12 关键点分组检查 + 边缘检测 + 状态防抖）
- 身体左右侧倾角（肩髋中点连线与垂直方向夹角，镜像校正）
- 双手间距与肩宽比（归一化坐标二维距离比）
- 指标数值 EMA 平滑，状态 5 帧防抖
- 基础动作指标面板显示

### V2 — 木棍/桨杆识别原型（已通过）
- 粉绿双色标记检测（HSV 阈值分割 + 连通域分析 + 候选评分）
- 低分辨率离屏 Canvas（320×180）限频检测（~12fps）
- 人体包围框扩展搜索区域
- 杆体方向角计算（镜像校正，复用 V1 配置）
- 5 种杆体识别状态 + 3 帧防抖
- Canvas 红蓝标记 + 金色方向线可视化
- 标记检测性能记录

### V3 — 静态握桨姿势评价（已编码，V4 中由 featureFlags 关闭）
- 手腕到杆体线段最短距离（点→段投影，归一化为倍数肩宽）
- 三级距离状态：靠近杆体 / 接近边界 / 偏离杆体
- 运动稳定性滑动窗口（手腕位移、杆体/身体角度、间距比变化）
- 姿势可分析条件判定（5 项条件 + 具体阻断原因提示）
- 时间基状态机：500ms 进入 / 200ms 退出（performance.now）
- V3 反馈面板（正式分析 / 预热中 / 条件不满足）

### V4.1 — 纯人体徒手划桨动作指标层（✅ 已测试通过）
- 新增 `featureFlags.ts`（V2/V3 关闭，V4 开启）
- 新增 `src/bodyStroke/` 目录（types / config / roles / metrics / tracker）
- 自动侧推断（基于手腕 Y 差），手动选择已移除
- 正确角色映射：划右侧=左手为上手上举，右手为下手拉桨
- 20 项人体动作指标（5 角度 + 5 距离 + 5 速度 + 2 身体中心 + 2 质量 + 1 派生）
- 所有位置/速度归一化为"倍肩宽"/"倍肩宽/秒"
- ScalarTracker：EMA 平滑 + 速度差分 + 丢失恢复（≥3 帧清空）
- BodyCenterCalibrator：30 帧中位数基线 + 稳定性检查 + 手动重置
- DebugBuffer：按 D 导出最近 300 帧 JSON（不含图像）
- 每项指标标注 reliability / interpretation / limitations
- 指标与五阶段状态机分离（V4.2 再开发）
- T1-T3 实物测试通过 ✅（2026-07-13）
- minDirectionSpeed 校准为 0.3（实测 P25=0.72）
- 下手方向评语阈值改为 45°-135°（拉桨方向 ±90° 正常）

### V4.2 — 五阶段划桨状态机（待设计）
- 基于 V4.1 的 20 项指标设计状态机
- 五阶段：准备 → 拉桨 → 推桨 → 恢复 → 暂停
- 使用 ScalarTracker 平滑数据进行状态判定
- 阈值基于 V4.1 实测数据
