# 会话总结

日期：2026年6月21日

---

## 完成的工作

### V0 — 人体姿态识别与稳定性优化（通过测试）
1. 修复视频帧重复推理导致的骨架跳变（`video.currentTime` 帧缓存去重）
2. 实现上肢关键点（肩/肘/腕）自适应 EMA 平滑（原始数据与显示数据分离）
3. 低置信度保留帧 + 透明度淡出绘制
4. 更新 TESTING.md 记录测试结果

### V1 — 基础人体指标识别（通过测试）
1. 新增 `poseMetrics.ts`：人体完整入镜（12 关键点分组检查 + 边缘检测）、身体侧倾角（镜像校正）、双手间距与肩宽比
2. 新增 `metricStabilizer.ts`：状态防抖（5 帧）+ 数值 EMA 平滑
3. 指标面板集成到页面
4. 更新 V1_TESTING.md 记录测试结果

### 部署准备
1. 新增 `visibilitychange` 监听：页面后台自动停止摄像头、释放资源
2. 隐私提示："摄像头画面仅在当前设备浏览器中实时处理，不会上传或保存。"
3. 移动端样式优化（按钮触摸区域、横竖屏适配、指标文本换行）
4. 创建 PROJECT.md（版本记录）、MOBILE_TESTING.md
5. 项目方向调整：主要使用设备改为电脑，暂时不验收移动端

### V2 — 木棍/桨杆识别原型（通过测试）
1. 新增 `markerDetector.ts`：HSV 阈值分割（H 0~360°）、连通域 BFS、候选组合评分
2. 新增 `stickMetrics.ts`：杆体方向角计算（镜像校正）
3. 新增 `stickStabilizer.ts`：状态防抖（3 帧）+ 角度 EMA 平滑 + 丢失超时
4. 粉绿双色标记检测（保留旧红蓝阈值注释）
5. 人体包围框扩展搜索 + 离屏 320×180 限频 ~12fps + 性能记录
6. Canvas 粉绿圆点 + 金色方向线绘制
7. 更新 V2_TESTING.md 记录测试结果
8. 从红蓝配色切换到粉绿配色（适配实际胶带）

### V3 — 静态握桨姿势评价（编码完成，待测试）
1. 新增 `postureMetrics.ts`：点到线段最短距离（投影参数 t 限制）、手腕距杆体归一化 + 三级分类、运动稳定性滑动窗口（~500ms）、姿势可分析条件判定（6 项）
2. 新增 `postureStabilizer.ts`：时间基状态机（500ms 进入 / 200ms 退出 / 短暂波动不切换）
3. 集成到 App.tsx：3 个状态 + 3 个 refs、V3 反馈面板（正式分析态 / 预热态 / 阻断原因）
4. 创建 V3_TESTING.md

---

## 当前项目状态

| 维度 | 状态 |
|------|------|
| 项目根目录 | `/d/Projects/sup-ai-coach` |
| 开发服务器 | http://localhost:5173/ |
| 构建 | `npm run build` ✅ 通过 |
| 部署 | 可通过 Vercel 一键部署 |

### 文件统计

```
src/
├── camera.ts             # 摄像头控制
├── poseDetector.ts       # MediaPipe 姿态检测（含帧去重）
├── drawing.ts            # Canvas 骨架绘制 + 杆体标记绘制
├── smoothing.ts          # 上肢 EMA 平滑
├── poseMetrics.ts        # V1 人体指标计算
├── metricStabilizer.ts   # V1 防抖与平滑
├── markerDetector.ts     # V2 颜色标记检测
├── stickMetrics.ts       # V2 杆体方向角
├── stickStabilizer.ts    # V2 状态防抖
├── postureMetrics.ts     # V3 姿势指标 + 稳定性
├── postureStabilizer.ts  # V3 时间基状态机
├── App.tsx               # 主组件
├── App.css               # 样式
├── index.css             # 全局样式
├── main.tsx              # 入口
└── vite-env.d.ts         # Vite 类型声明
```

---

## 已通过阶段

| 版本 | 状态 | 验证方式 |
|------|------|---------|
| V0：人体姿态识别与稳定性优化 | ✅ 通过 | 实物测试，骨架绘制、镜像、FPS、资源释放 |
| V1：基础人体指标识别 | ✅ 基本通过 | 实物测试，完整入镜、侧倾角、间距比 |
| V2：木棍/桨杆识别原型 | ✅ 通过 | 实物测试，粉绿标记识别、杆体方向角 |
| V3：静态握桨姿势评价 | 🔧 编码完成待测试 | 待实物测试验证 |

---

## 下一步待办

1. **V3 实物测试** — 使用粉绿胶带杆体进行测试，填写 V3_TESTING.md
2. **V3 阈值校准** — 根据实测调整稳定性阈值和手腕距杆体三级阈值
3. **V4 规划** — 待 V3 通过后讨论
4. **Vercel 部署**（可选）— 推送到 GitHub 并部署到 Vercel 获取公网 HTTPS 链接
