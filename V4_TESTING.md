# V4.1 纯人体徒手划桨动作指标 — 测试方案

> 日期：2026-06-23  
> 分支：`feature/body-stroke-analysis-v4`  
> 状态：编码完成，待实物测试

---

## 测试目标

1. 验证 20 项指标的输出是否稳定合理（不断帧、NaN、突变）
2. 验证侧选 UI 切换正确改变角色映射
3. 验证下手位置在拉桨至髋时的范围合理性
4. 收集各指标在真实模拟动作中的 min/max/mean/std
5. 确定 auto-inference 的基本参考值（仅调试用）

## 测试序列

### T1 — 静态校准（测量噪声基线）

| 测试 | 动作 | 时长 | 检查要点 |
|------|------|------|---------|
| T1-1 | 站立不动，面朝镜头 | 10s | 各指标基线噪声水平 |
| T1-2 | 侧身 45° 站立不动 | 10s | 偏转后指标变化 |
| T1-3 | 深蹲/微屈膝 | 5s | 膝角变化范围 |

### T2 — 静态握桨位（侧判据验证）

| 测试 | 动作 | 检查要点 |
|------|------|---------|
| T2-1 | 选择右侧，做模拟握桨位（左手上举、右手拉至右髋） | 下手在上手下方（Y 更大） |
| T2-2 | 选择左侧，做模拟握桨位（右手上举、左手拉至左髋） | 角色映射正确切换 |
| T2-3 | 右侧→左侧切换 | UI 切换后角色重映射 |

### T3 — 动态模拟划桨（核心测试）

| 测试 | 动作 | 次数 | 数据收集 |
|------|------|------|---------|
| T3-1 | 单次右侧慢速划桨 | ×5 | 逐帧导出 debug JSON |
| T3-2 | 连续右侧划桨 | ×10 | 稳定周期数据 |
| T3-3 | 单次左侧慢速划桨 | ×5 | 同上 |
| T3-4 | 连续左侧划桨 | ×10 | 同上 |

### T4 — 边界情况

| 测试 | 动作 | 检查要点 |
|------|------|---------|
| T4-1 | 一侧手臂抬起（模拟抓挠） | 不输出错误指标 |
| T4-2 | 快速连续划桨 | 速度不饱和 |
| T4-3 | 侧对镜头 | 二维限制不误判 |

## 数据收集

每轮 T3 测试后按 <kbd>D</kbd> 键导出 JSON。

文件命名：`v41-debug-{test-id}-{datetime}.json`

导出内容包含：
- `configVersion`：`"v4.1.0"`
- `selectedSide`：选定划桨侧
- `frames`：最多 300 帧，每帧含所有指标值 + validity + 时间戳
- **不含图像数据**

## 验收标准（首轮）

- ✅ 所有帧不出现 NaN / Infinity / undefined
- ✅ 时间戳单调递增，deltaTimeMs 合理
- ✅ 静止站立时无明显周期跳变
- ✅ 丢失 ≥ 3 帧后恢复，速度不产生巨大尖峰（> 100 倍肩宽/秒）
- ✅ 侧选切换后角色映射正确
- ✅ 连续多次动作曲线形状基本一致
- ✅ JSON 导出完整、单位正确
- ❌ 不要求 auto-inference 准确率
- ❌ 不要求阈值判定

## 导出 JSON 结构示例

```json
{
  "configVersion": "v4.1.0",
  "exportedAt": 1234567890.123,
  "selectedSide": "right",
  "frameCount": 300,
  "frames": [
    {
      "timestamp": 1234567890.123,
      "frameTime": 12.34,
      "deltaTimeMs": 33.3,
      "selectedSide": "right",
      "metrics": {
        "elbowAngleDeg": { "left": 165.2, "right": 92.1 },
        "kneeAngleDeg": { "left": 172.0, "right": 168.3 },
        "handSpanRatio": 1.35,
        "powerWristRelativeHorizontalVelocity": -0.05,
        "powerWristRelativeCompositeSpeed": 0.13,
        "validity": {
          "elbowAngleLeft": true,
          "powerWristRelDirection": false
        }
      }
    }
  ]
}
```

---

## 测试结果（2026-07-13 执行）

### 执行状态

| 测试 | 状态 | 结果 |
|------|------|------|
| T1-1 正面静止 | ✅ 通过 | 噪声基线：躯干侧倾 ±0.5°，身体稳定性 ±0.003×肩 |
| T1-2 侧身 45° | ✅ 通过 | 肩髋投影差正确捕捉旋转偏差（6°→22°） |
| T1-3 深蹲 | ⏭ 跳过 | 后续 V4.2 再测 |
| T2-1 右侧握桨位 | ✅ 通过 | 下支撑手肘 141°(下手)，上支撑手肘 111°(上手)，角色映射正确 |
| T2-2 左侧握桨位 | ⚠️ 部分通过 | 角色映射正确（人工判断），自动侧判未切换 |
| T3-1/2 右侧动态 | ✅ 通过 | 300 帧，91% 指标覆盖率，方向角 ~+90° 符合拉桨规律 |
| T3-3/4 左侧动态 | ✅ 通过 | 300 帧，92-97% 覆盖率，方向角 ~-90° 镜像对称 |
| T4 边界情况 | ⏭ 跳过 | 后续再测 |

### 校准的阈值

| 参数 | 旧值 | 新值 | 依据 |
|------|------|------|------|
| `minDirectionSpeed` | 0.05 | **0.3** | T3 动态划桨 P25 速度=0.72，0.3 滤掉 5% 低噪帧 |
| 下手方向评语 | ≤30°→合理 | **45°-135°→合理** | 实测拉桨方向在 ±90°，30° 阈值误报 |

### 发现的 Bug

1. **报告被覆盖** — 15s 计时器继续运行覆盖定格数据 ✅ 已修复
2. **校准基线被清空** — `resetAll()` 在开始测试时清除了校准基线 ✅ 已修复
3. **debug JSON 闭包陈旧值** — `latestDebugRef` 捕获了旧的 `metricsLean`/`metricsHandRatio` ✅ 已修复

